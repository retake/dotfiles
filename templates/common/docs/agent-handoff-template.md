# Agent Handoff Template — Pointer Stub

This file has been migrated to the workflow package. It is no longer maintained here.

**Canonical source**: `~/dev/ai-agent-workflow/templates/agent-handoff-template.md`

The workflow package at `~/dev/ai-agent-workflow/` is the single source of truth for
handoff templates. Use the package template directly or copy it for new projects.

Reason: migrated from `templates/common/`; the package is now the single source of truth.
Slice: HO-W005 / 2026-05-06 (parent consult HO-W003).
